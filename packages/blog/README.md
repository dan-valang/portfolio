# portfolio-blog

Blog application built with TanStack Start.

To run development server: `npm run -w blog dev`  
To build for production: `npm run -w blog build`  

This package is a placeholder and will be scaffolded with full TanStack Start project structure in subsequent implementation steps.