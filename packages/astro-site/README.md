# astro-site

Main portfolio website built with Astro.

To run development server: `npm run -w astro-site dev`  
To build for production: `npm run -w astro-site build`  

This package is a placeholder and will be scaffolded with full Astro project structure in subsequent implementation steps.